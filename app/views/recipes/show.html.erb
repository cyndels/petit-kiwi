<h1><%= @recipe.name %></h1>

<p>Temps de cuisson : <%= @recipe.cooking_time %>min</p>
<p>Pour :
  <% if @recipe.party == 1 %>
    <%= @recipe.party %> personne
  <% else %>
    <%= @recipe.party %> personnes
  <% end %>
  - <%= link_to "Modifier les quantités", recipe_resize_path(@recipe) %>
</p>

<%= link_to "Ajouter à une wishlist", new_recipe_item_path(@recipe) %>

<h2>Instructions</h2>

<ol>
  <% @recipe.ingredients.each do |ingredient| %>
    <li><%= ingredient.quantity %> <%= ingredient.unit %> <%= ingredient.name %></li>
  <% end %>
</ol>

<%= link_to "Modifier les ingrédients", new_recipe_ingredient_path(@recipe) %>

<h2>Instructions</h2>

<ol>
  <% @recipe.instructions.each do |instruction| %>
    <li><%= instruction.content %></li>
  <% end %>
</ol>

<%= link_to "Modifier les instructions", new_recipe_instruction_path(@recipe) %>
<br>
<br>
<%= link_to "Supprimer cette recette", recipe_path(@recipe), method: :delete, data: { confirm: "Supprimer cette recette ?" } %>
<br>
<%= link_to "Retour au dashboard", dashboard_path %>
