<%= simple_form_for [@recipe, @dose] do |f| %>
  <%= f.input :quantity, label: "Quantité" %>
  <%= f.input :unit, label: "Unité", collection: Dose.units.keys %>
  <%= f.association :ingredient %>
  <%= f.submit "Ajouter cet ingredient" %>
<% end %>
